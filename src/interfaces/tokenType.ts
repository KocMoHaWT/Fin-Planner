export enum TokenType { 
    refresh = 'refreshToken',
    access = 'accessToken',
    google = 'googleToken',
    apple = 'appleToken'
}

export enum JWTType {
    access = 'access',
    refresh = 'refresh'
}